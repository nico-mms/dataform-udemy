config {
    type: "incremental",
    uniqueKey: ["meal_id", "drink_id"]
}

SELECT DISTINCT
  drink_id,
  meal_id,
  drink_name,
  meal_name,
FROM
  ${ref("drinks")} as drinks
JOIN
  ${ref("meals")} as meals
USING
  (drink_id)
GROUP BY
  ALL
